pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/nsr94/hello-world.git'
                echo 'Building...'
            }
        }   
    }
}